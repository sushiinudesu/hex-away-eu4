hex_add_core = {
	cede_province = ROOT
	add_core = ROOT
	change_culture = ROOT
	change_religion = ROOT
	change_trade_goods = hex_trade_good_none
}

hex_add_to_city = {
	if = {
		limit = { hex_native_mechanic_available = yes }
		change_tribal_land = ROOT
		change_culture = ROOT
		change_religion = ROOT
		change_trade_goods = hex_trade_good_none
	}
	else = { hex_add_core = yes }
}