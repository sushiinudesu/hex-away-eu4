hex_setup_world = {
	if = {
		limit = { NOT = { has_country_flag = hex_setup_finished } }

		every_province = { set_in_empire = no }

		if = { limit = { is_year = 7 } set_global_flag = hex_basis_arc_flag }
		if = { limit = { is_year = 13 } set_global_flag = hex_control_arc_flag }
	}
}