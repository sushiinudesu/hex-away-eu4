hex_artisan_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_ARTISANS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
        }
    }

	modifier = { }
}

hex_builders_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_BUILDERS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
		}
    }

	modifier = { }
}

hex_farmers_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_FARMERS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
        }
    }

	modifier = { }
}

hex_hunters_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_HUNTERS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
        }
    }

	modifier = { }
}

hex_miners_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_MINERS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
        }
    }

	modifier = { }
}

hex_sailors_guild = {
	always = yes
	monarch_power = DIP

	allow = { tag = A01 }

	triggered_faction_name = {
        name = HEX_SAILORS
        
        trigger = {
			NOT = { has_global_flag = hex_basis_arc_flag }
        }
    }

	modifier = { }
}