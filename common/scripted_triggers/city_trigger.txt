hex_owned_by_city = {
	if = {
		limit = { hex_native_mechanic_available = yes }
		OR = {
			owned_by = ROOT
			AND = {
				tribal_land_of = ROOT
				is_city = no
			}
		}
	}
	else = { owned_by = ROOT }
}