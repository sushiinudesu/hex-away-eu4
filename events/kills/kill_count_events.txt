namespace = hex_kill_count_events

country_event = {
	id = hex_kill_count_events.1
	title = hex_kill_count_events.1.t
	desc = hex_kill_count_events.1.d
	
	picture = HEX_EVENT_PICTURE_HAWK_AWAKE

	is_triggered_only = yes

	trigger = { ai = no }

	option = {
		name = hex_kill_count_events.1.a
	}

	after = {
		clr_country_flag = hex_kill_count_open_flag
	}
}