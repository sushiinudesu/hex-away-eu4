namespace = hex_city_missions_chef_events

country_event = {
	id = hex_city_missions_chef_events.1
	title = hex_city_missions_chef_events.1.t
	desc = hex_city_missions_chef_events.1.d
	picture = CAVE_eventPicture
	
	is_triggered_only = yes
	
	option = {
        name = hex_city_missions_chef_events.1.a
		custom_tooltip = hex_city_missions_chef_events.1.tt

        ai_chance = { factor = 100 }
	}
}
