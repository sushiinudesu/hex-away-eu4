hex_missions_city_longbeard_awakening_left = {
	slot = 1

	ai = yes
	generic = no
	has_country_shield = yes

	potential = {
		tag = A01
		faction_in_power = hex_miners_guild
	}
	
	hex_missions_city_longbeard_silver_vein = {
		icon = mission_sio_camp_circles
		position = 1
		required_missions = { }

		trigger = {
			custom_trigger_tooltip = {
				tooltip = HEX_IS_EARTH_SEASON
				hex_is_earth_season = yes
			}
		}

		effect = {
			country_event = { id = hex_city_missions_longbeard_events.1 }

			hidden_effect = {
				add_faction_influence = { faction = hex_miners_guild influence = 1 }
			}
		}
	}
}

hex_missions_city_longbeard_awakening_right = {
	slot = 5

	ai = yes
	generic = no
	has_country_shield = yes

	potential = {
		tag = A01
		faction_in_power = hex_miners_guild
	}
	
	hex_missions_city_longbeard_city_mine = {
		icon = mission_sio_camp_circles
		position = 1
		required_missions = { }

		trigger = {
			15 = {
				owned_by = ROOT
				has_great_project = { type = hex_project_city_mine tier = 0 }
			}
		}

		effect = {
			hidden_effect = {
				add_faction_influence = { faction = hex_miners_guild influence = 1 }
			}
		}
	}
}