hex_missions_city_lion_awakening_left = {
	slot = 1

	ai = yes
	generic = no
	has_country_shield = yes

	potential = {
		tag = A01
		faction_in_power = hex_hunters_guild
	}
	
	hex_missions_city_lion_the_hunt = {
		icon = mission_sio_camp_circles
		position = 1
		required_missions = { }

		trigger = {
			custom_trigger_tooltip = {
				tooltip = HEX_IS_WIND_SEASON
				hex_is_wind_season = yes
			}
		}

		effect = {
			country_event = { id = hex_city_missions_lion_events.1 }

			hidden_effect = {
				add_faction_influence = { faction = hex_hunters_guild influence = 1 }
				hex_mission_reward = yes
			}
		}
	}
}

hex_missions_city_lion_awakening_right = {
	slot = 5

	ai = yes
	generic = no
	has_country_shield = yes

	potential = {
		tag = A01
		faction_in_power = hex_hunters_guild
	}
	
	hex_missions_city_lion_lion_fort = {
		icon = mission_sio_camp_circles
		position = 1
		required_missions = { }

		provinces_to_highlight = {
			province_id = 17
		}

		trigger = {
			17 = {
				owned_by = ROOT
				has_great_project = { type = hex_project_lion_fort tier = 0 }
			}
		}

		effect = {
			hidden_effect = {
				add_faction_influence = { faction = hex_hunters_guild influence = 1 }
				hex_mission_reward = yes
			}
		}
	}
}